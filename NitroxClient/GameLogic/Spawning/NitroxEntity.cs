using UnityEngine;

namespace NitroxClient.GameLogic.Spawning
{
    /**
     * 
     * Class to simply identify objects that were spawned by Nitrox.
     * This class was originally created to prevent over aggressive 
     * deletion of client side entities; however, it can later be 
     * extended to other use-cases.
     * 
     */
    public class NitroxEntity : MonoBehaviour
    {
    }
}
