namespace Nitrox.Model.Constants
{
    /// <summary>
    /// Values for the unconnected request / response used by the in-game server list
    /// to learn a server's settings before joining
    /// </summary>
    public static class ServerInfoConstants
    {
        public const string INFO_REQUEST_STRING = "NitroxServerInfoRequest";
        public const string INFO_RESPONSE_STRING = "NitroxServerInfoResponse";
    }
}
