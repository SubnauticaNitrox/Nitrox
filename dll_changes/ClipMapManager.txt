/* ShowMeshes() after if(ClipMapmanager) {} */
if(Multiplayer.isActive) {
    Multiplayer.client.AddChunk(this.chunk.transform.position);
}

/* HideMeshes() after if(ClipMapmanager) {} */
if(Multiplayer.isActive) {
    Multiplayer.client.RemoveChunk(this.chunk.transform.position);
}